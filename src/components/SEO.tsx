import { Helmet } from 'react-helmet-async';

interface SEOProps {
  title?: string;
  description?: string;
  keywords?: string;
  image?: string;
  url?: string;
  type?: string;
}

const SEO: React.FC<SEOProps> = ({
  title = "Iskren Iliev - Frontend Developer & Full-Stack Engineer",
  description = "Professional Frontend Developer specializing in ReactJS, AngularJS, NodeJS, and TypeScript. Creating modern, responsive web applications with exceptional user experience.",
  keywords = "frontend developer, reactjs developer, angularjs developer, nodejs developer, typescript expert, full-stack developer, web developer, portfolio, sofia bulgaria",
  image = "https://iskreniliev.dev/og-image.jpg",
  url = "https://iskreniliev.dev",
  type = "website"
}) => {
  return (
    <Helmet>
      {/* Primary Meta Tags */}
      <title>{title}</title>
      <meta name="title" content={title} />
      <meta name="description" content={description} />
      <meta name="keywords" content={keywords} />
      
      {/* Open Graph / Facebook */}
      <meta property="og:type" content={type} />
      <meta property="og:url" content={url} />
      <meta property="og:title" content={title} />
      <meta property="og:description" content={description} />
      <meta property="og:image" content={image} />
      
      {/* Twitter */}
      <meta property="twitter:card" content="summary_large_image" />
      <meta property="twitter:url" content={url} />
      <meta property="twitter:title" content={title} />
      <meta property="twitter:description" content={description} />
      <meta property="twitter:image" content={image} />
    </Helmet>
  );
};

export default SEO;
